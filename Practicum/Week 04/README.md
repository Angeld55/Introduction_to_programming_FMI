# УП - Практикум 29.10.2025 Седмица 4 

**Задача 1:** Да се напише функция `absoluteValue(int number)`, която приема число и връща абсолютната му стойност.

**Пример:**
```c++
cout << absoluteValue(-7) << endl;
cout << absoluteValue(12) << endl;
```
Изход:
```c++
7
12
```

**Задача 2:** Напишете функция `isDigit(char symbol)`, която приема символ и определя дали е число.

*Bonus: Направете функции `isLower` и `isUpper` за проверка на главни и малки букви*

**Пример:**
```c++
cout << isDigit('а') << endl;
cout << isDigit('0') << endl;
cout << isDigit('B') << endl;
```
Изход:
```c++
false, lower letter
true
false, upper letter
```

**Задача 3:** Напишете функции за преобразуване на главни и малки букви. 
* `toUpper(char symbol)` - превръща малка буква в главна буква
* `toLower(char symbol)` - превръща главна буква в малка буква

*Не забравяйте валидацията на входните данни!*

**Пример:**
```c++
cout << toUpper('a') << endl;
cout << toLower('A') << endl;
```
Изход:
```c++
'А'
'а'
```

**Задача 4:** Напишете функции за конвертиране между символ в цифра.
* `toNumber(char symbol)` - конвертира от символ в цифра
* `toCharacter(int number)` - конвертира от цифра в символ

**Пример:**
```c++
cout << toNumber('1') << endl;
cout << toCharacter(3) << endl;
```
Изход:
```c++
1
'3'
```

**Задача 5:** Да се напише функция `power(int base, unsigned int exponent)`, която повдига подаденото число на подадената степен.

**Пример:**
```c++
cout << power(5, 2);
```
Изход:
```c++
25
```

**Задача 6:** Да се напише функция `getLength(int number)`, която връща колко цифри има даденото число.

**Пример:**
```c++
cout << getLength(123) << endl;
cout << getLength(0) << endl;
```
Изход:
```c++
3
1
```


**Задача 7:** Да се напише функция `lcm(int first, int second)`, която връща най-малкото общо кратно на две числа.

**Пример:**
```c++
cout << lcm(6, 9) << endl;
```
Изход:
```c++
18
```

**Задача 8:** Да се напише функция `getDigitOnPosition(int number, unsigned int position)`, която връща цифрата от числото на подадената позиция.
**Валидирайте входните данни!**

**Пример:**
```c++
cout << getDigitOnPosition(6289, 3) << endl;
```
Изход:
```c++
8
```

**Задача 9:** Напишете функция `concat(int first, int second)`, която връща число, получено при конкатенация (долепяне) на first и second (положителни цели числа). 

*Направете нужните валидации на входните данни!*

**Пример:**
```c++
cout << concat(123, 456);
```
Изход:
```c++
123456
```

**Задача 10:** Да се напише функция, която като параметри приема две числа *m* и *n* (нека *m* < *n*) и връща сумата в интервала [m, n].

**Пример:**
```c++
cout << sumInInterval(10, 15);
```
Изход:
```c++
75
```

**Задача 11:** Да се напише функция `isSquareRoot(int number)`, която проверява дали даденото число е точен корен.

**Пример:**
```c++
cout << isSquareRoot(24) << endl;
cout << isSquareRoot(25) << endl;
```
Изход:
```c++
false
true
```

**Задача 12:** Да се напише функция `sortDigits(int number)`, която сортира цифрите на подадено цяло положително число.

*Направете нужните валидации!*

**Пример:**
```c++
cout << sortDigit(1234) << endl;
cout << sortDigit(6253) << endl;
```
Изход:
```c++
1234
2356
```

**Задача 13:** Създайте функции, които приемат координатите на три точки:
* `getPerimeter(int x1, int y1, int x2, int y2, int x3, int y3)` - Намира обиколката на триъгълника дефиниран от тях
* `getArea(int x1, int y1, int x2, int y2, int x3, int y3)` - Намира лицето на триъгълника дефиниран от тях
* `isInCircleWithRadius(int x1, int y1, int x2, int y2, int x3, int y3, int radius)` - Проверява дали триъгълникът дефиниран от тях се намира изцяло в окръжност с радиус *r* и център (0, 0). Радиусът се подава като параметър на функцията.

**Пример:**
```c++
cout << getPerimeter(0, 0, 3, 0, 3, 4) << endl;
cout << getArea(0, 0, 3, 0, 3, 4) << endl;
cout << isInCircleWithRadius(0, 0, 3, 0, 3, 4, 10) << endl;
```
Изход:
```c++
perimeter: 12 
area: 6
is in circle: true
```

*Следете за повторение на код. Може да се наложи да обособите повече от две функции, които не са упоменати в условието!*

**Задача 14:** Създайте функция `isDivisableByValue(int number, int divisor)`, която проверява дали *number* се дели с остатък 0 на *divisor*. **Не може да използвате оператор % за тази задача!**

**Пример:**
```c++
cout << isDivisableByValue(6, 3)<< endl;
cout << isDivisableByValue(14, 5)<< endl;
```
Изход:
```c++
true
false
```

**Задача 15:** Да се напише функция `getRemainder(int first, int second)`, която играе ролята на оператор % (за делене с остатък), т.е. връща като резултат *first* % *second*. **Не може да използвате оператор % за тази задача!**

**Пример:**
```c++
cout << getRemainder(5, 2);
```
Изход:
```c++
1
```

**Задача 16:** Да се напише функция `getDivision(int first, int second)`, която играе ролята на оператор / (за целочислено делене), т.е. връща като резултат *first* / *second*. **Не може да използвате оператор / за тази задача!**

**Пример:**
```c++
cout << getDivision(30, 7);
```
Изход:
```c++
4
```

**Задача 17:** Да се напише функция `squareRootToLower(int number)`, която връша корен от *number*, закръглен надолу. **Не може да използвате sqrt() за тази задача!**

**Пример:**
```c++
cout << squareRootToLower(24)<< endl;
```
Изход:
```c++
4
```

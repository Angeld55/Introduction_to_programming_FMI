#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
    // 14 = 000...001110 - така с "побитово и" ще вземем само тези 3 бита, а останалите ще бъдат 0. После ги преместваме до началото (използвайки >>).
	cout << ((n & 14) >> 1);
}